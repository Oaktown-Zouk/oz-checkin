{
  "name": "oz-checkin",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev:server": "npm run dev --workspace server",
    "dev:web": "npm run dev --workspace web",
    "dev:netlify": "netlify dev --filter web",
    "dev:sandbox": "PORT=3000 MOCK_AIRTABLE=true DEV_LOGIN_ENABLED=true APP_ORIGIN=http://localhost:5199 npm run dev:server & npm run dev --workspace web -- --port 5199",
    "build": "npm run build --workspace web && npm run build --workspace server",
    "typecheck": "npm run typecheck --workspace server && npm run typecheck --workspace web && tsc --noEmit -p netlify/tsconfig.json",
    "test": "npm run test --workspace server",
    "test:e2e": "playwright test --config=e2e/playwright.config.ts"
  },
  "devDependencies": {
    "@playwright/test": "1.48",
    "netlify-cli": "^27.1.2"
  }
}
