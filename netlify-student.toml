[build]
  command = "npm run build"
  publish = "web-student/dist"

[functions]
  directory = "netlify/functions-student"
  node_bundler = "esbuild"

# Same SPA-fallback reasoning as the staff app's netlify.toml — see its comment.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
